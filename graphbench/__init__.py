"""graphbench: a clean-room, multi-engine graph query benchmark.

A single harness drives every engine through a common adapter interface, validates
that engines return the same row-set before timing them, and reports structured
results. See the package README for usage.
"""

__version__ = "0.1.0"
